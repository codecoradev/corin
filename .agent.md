# Agent Context — CorIn

> **This file is permanent context for AI agents. Read it fully before coding.**

## Project

CorIn — Cora Intelligence. Desktop knowledge workstation.
Local-first, offline-capable Tauri 2 + Svelte 5 + Rust + SQLite app for managing
memories, knowledge graphs, and rooms.

## Stack

| Layer | Tech |
|-------|------|
| Desktop shell | Tauri 2 |
| Frontend | Svelte 5 (runes: `$state`, `$derived`, `$effect`, `$props`) |
| Backend | Rust (edition 2024) |
| Storage | SQLite via rusqlite 0.40 (bundled) |
| Graph | D3.js v7 (force-directed) |
| Markdown | CodeMirror 6 + marked + dompurify |
| Theme | Catppuccin Mocha (dark) |

## Architecture

```
Svelte 5 Frontend (src/)
  └── Tauri IPC (invoke) — src/lib/ts/ipc.ts
       └── Rust Commands — src-tauri/src/commands.rs
            ├── UtekeClient (HTTP API) — src-tauri/src/uteke_client.rs
            │    └── http://127.0.0.1:8767 (or remote domain)
            │         └── uteke-serve (semantic search, auto-link, graph)
            └── Hub DB (SQLite via rusqlite)
                 └── CorIn-native settings + local memories
```

### ⚠️ CRITICAL: All Uteke Access via HTTP API Only

**NEVER open a direct SQLite connection to `~/.uteke/uteke.db`.**
All Uteke data (memories, graph, rooms, stats, search) must go through
the HTTP client (`uteke_client.rs` → `http://127.0.0.1:8767`).

Reasons:
1. **Always fresh** — HTTP reads latest DB state, no stale cache
2. **Future-proof** — server URL can be `localhost` or `https://uteke.yourdomain.com`
3. **Consistency** — delete via HTTP + stats via HTTP = data always sync
4. **Remote support** — desktop app can connect to remote uteke-serve

If tempted to add `rusqlite::Connection::open("~/.uteke/uteke.db")` — **STOP**.
Use `UtekeClient` instead.

### ⚠️ CRITICAL: Room/CRUD commands MUST use HTTP client (not native store)

The native store (`state.uteke` / `ensure_uteke()`) is **never initialized** —
`s.uteke` is always `None`. Only `s.uteke_client` (the HTTP client) is set
in `setup()`. So:

- **WRONG:** `s.ensure_uteke()?.store().create_room(...)` →
  fails with `"uteke store not initialized"`
- **RIGHT:** clone the HTTP client out of the lock, then call the HTTP method:
  ```rust
  let client = {
      let s = state.lock().await;
      s.uteke_client.clone()
  }.ok_or(CommandError::NotInitialized)?;
  client.room_create(&id, Some(&name), ns).await.map_err(CommandError::Uteke)?;
  ```

Every room/CRUD command must follow this clone-client-out-of-lock pattern.

### Response decoding gotcha

`uteke-serve` returns JSON **objects** (e.g. `{"created":"...","namespace":"..."}`),
not plain strings. When adding client methods:
- Deserialize to `serde_json::Value` (safe, flexible), OR
- Define a typed struct matching the response
- **NEVER** deserialize to `String` — it will fail with
  `"error decoding response body"`

### Connection layer (Phase 6+)

Remote/local connections live in the `connections` table and are managed via the
`connections/` module.  Rules:

1. **Active backend = `AppState.uteke_client`.**  It is rebuilt from the primary
   connection by `rebuild_active_client()` / the `reconnect_connection(id)`
   command.  Switching primary (`set_primary_connection`) **also rebuilds the
   live client** — never update the DB row without swapping the client.
2. **Local vs remote** is decided by `config::is_remote_url()`.  Local URLs
   trigger `ensure_uteke_server()` (install + spawn); remote URLs skip spawn.
3. **Auth tokens are secret.**  Never log a token verbatim — use
   `mask_token_log()`.  On `delete_connection`, `store::clear_token()` wipes the
   column to NULL **before** the row is deleted (prevents forensic recovery).
4. **DB file perms** are set to `0600` on startup (Unix) because the db holds
   tokens.
5. **Adding a product** = new `ProductType` variant + adapter impl + register in
   `build_adapter()`.  Do not touch connection CRUD/persistence/UI plumbing.
6. **Disconnect vs reconnect.** `disconnect_connection` drops
   `AppState.uteke_client` to `None` (recall/search fail until reconnect) but
   keeps the primary row + flag.  `reconnect_connection(id)` rebuilds it live.
7. **Set Primary guard.** The UI only offers "Set Primary" on connections whose
   status is `connected`, and "Disconnect" only on the connected primary.
8. **No native `confirm()`/`alert()`** — Tauri's webview blocks them. Use an
   in-app modal (see the `confirm-overlay` / `confirm-dialog` pattern in
   `ConnectionManager.svelte`).

### Error handling pattern (frontend CRUD)

**NEVER** silently swallow errors with `catch { }`. Every CRUD operation must:
1. `catch (e)` and extract the message
2. `console.error(op + ' failed', e)`
3. Surface to the user via an error banner/state (e.g. `reportError()` helper)

Silent catches hide real bugs (wrong endpoint, type mismatch, server down)
and make debugging impossible.

### Key files

- `src/App.svelte` — root component
- `src/lib/stores/ui.svelte.ts` — UI state (sidebar, panels, active view)
- `src/lib/stores/memories.svelte.ts` — memory CRUD store
- `src/lib/ts/ipc.ts` — Tauri invoke wrappers
- `src/lib/ts/types.ts` — shared types
- `src-tauri/src/commands.rs` — all Tauri commands
- `src-tauri/src/lib.rs` — Tauri builder + handler registration
- `src-tauri/src/uteke_client.rs` — HTTP client for uteke-serve API
- `src-tauri/src/config.rs` — `~/.codecora/` init + model path resolution

---

## ⛔ Critical Rules — MUST FOLLOW (read carefully)

### Rule 1: NEVER push directly to `develop` or `main`

All changes go through a feature branch → PR → CI green → merge.

```bash
# ✅ CORRECT
git checkout -b feat/05-dashboard
# ... work ...
git push origin feat/05-dashboard
gh pr create --base develop

# ❌ FORBIDDEN
git checkout develop
git commit -m "..."
git push origin develop   # NEVER DO THIS
```

**Branch naming:**
- Feature: `feat/<issue#>-<description>` (e.g. `feat/05-dashboard`)
- Fix: `fix/<description>`
- Chore: `chore/<description>`

### Rule 2: NEVER bypass cora review (`--no-verify` is FORBIDDEN)

The pre-commit hook runs `cora review --staged`. If cora finds issues:

```bash
# ✅ CORRECT — fix the issues, then commit again
# (review the cora output, fix the code, re-stage, commit)

# ❌ FORBIDDEN
git commit --no-verify   # NEVER DO THIS
```

If cora flags a false positive on generated files (e.g. `src-tauri/gen/`),
add them to `ignore.files` in `.cora.yaml` instead of bypassing.

### Rule 3: `cargo fmt` + `cargo clippy` before every commit

```bash
cd src-tauri && cargo fmt && cargo clippy --all-targets -- -D warnings
```

CI runs `--check` and `-D warnings` — any formatting or lint failure blocks the PR.

### Rule 4: One issue = one branch = one PR = wait CI green = merge = next

Do not combine multiple issues in one branch. Do not start the next issue
until the current PR is merged and CI is green.

### Rule 5: PRs always target `develop` (never `main`)

`main` is only for releases. Release flow: develop → PR to main → tag → release.

---

## Workflow (per issue)

```
1. git checkout develop && git pull
2. git checkout -b feat/<issue#>-<description>
3. ... implement ...
4. cd src-tauri && cargo fmt && cargo clippy --all-targets -- -D warnings
5. npm run build   # verify frontend compiles
6. git add <files>
7. git commit -m "feat: ..."   # cora review runs automatically
8. git push origin feat/<branch>
9. gh pr create --base develop --title "..." --body "Closes #N"
10. wait for CI green (all checks pass)
11. merge PR (squash)
12. git checkout develop && git pull
13. delete local branch
14. proceed to next issue
```

## Phase Plan

- **Phase 1 (v0.1):** ✅ MVP — memory CRUD, graph viz, search, rooms
- **Phase 2 (v0.2):** Agent integration (auto-start, wizard, doc engine, room UI)
  - ✅ Room management UI — create room, tabs (timeline/summary/participants), delete with confirm
  - All access via HTTP API (no direct SQLite)
  - Semantic search default (vector + FTS5 hybrid via RRF)
  - Cosine auto-linking (similar_to, possible_duplicate)
  - Dedup protection (score >= 0.92)
  - MCP integration for AI agents
- **Phase 3 (v0.3):** Multi-product dashboard, timeline, maintenance

## Key Rules for Agents

- **Svelte 5 runes** (`$state`, `$derived`, `$effect`) are correct — do NOT use Svelte 4 `$:` syntax
- **All SQL** must use parameterized queries (`rusqlite::params![]` or `?N` placeholders)
- **rusqlite 0.40:** `usize` does NOT implement `ToSql`/`FromSql` — always cast to `i64`
- **Tauri commands** are async fns with `#[tauri::command]`, state via `tauri::State<Arc<Mutex<AppState>>>`
- **Tauri arg naming:** Rust `snake_case` params are auto-converted to `camelCase` at the
  IPC boundary. Rust `room_id: String` → frontend must invoke with `{ roomId }`, NOT `{ room_id }`.
  Single-word params (`name`, `tags`) are unaffected. Mismatched keys produce:
  `invalid args ... missing required key roomId`
- **`Manager` trait** must be imported (`use tauri::Manager`) to use `get_webview_window`
- **IPC types** must stay in sync between `src/lib/ts/types.ts` and `src-tauri/src/commands.rs`
- **CommandError** serializes to string for frontend — add new variants carefully

## Code Review

- **Pre-commit hook:** `cora review --staged --format compact` runs automatically
- **CI:** `cora-review.yml` runs on every PR via `codecoradev/cora-review-action@v1`
- Config: `.cora.yaml` (provider, focus, rules, ignore patterns)
- Required CI secrets: `CORA_API_KEY`, `CORA_BASE_URL`, `CORA_MODEL`

## CI Required Checks

| Check | Job |
|-------|-----|
| Frontend Type Check | `npx tsc --noEmit` |
| Frontend Build | `npm run build` |
| Frontend Test | `npm test` (Vitest) |
| Rust Check | `cargo check --all-targets` |
| Rust Format | `cargo fmt --check` |
| Rust Clippy | `cargo clippy -- -D warnings` |
| Rust Test | `cargo test` |
| Cora Review | AI code review (blocking on errors) |

## Commit Messages

Follow Conventional Commits:

```
feat: add dashboard stats cards (#5)
fix: handle empty namespace in list command
docs: update README with architecture diagram
refactor: extract edge validation into helper
chore: bump dependencies
```

## Release Authority

**Owner approval required before ANY release.** Agent must:
1. Complete pre-release checklist
2. Present validation report
3. Wait for explicit approval
4. Only then: bump version + tag + push
